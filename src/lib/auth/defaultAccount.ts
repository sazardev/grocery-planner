/**
 * Cuenta fija de bypass sembrada por el backend al arrancar
 * (`AuthStore::seed_default_account`). Úsala para entrar sin registrar.
 */
export const DEFAULT_ACCOUNT = 'admin'
export const DEFAULT_PASSWORD = 'admin123'
